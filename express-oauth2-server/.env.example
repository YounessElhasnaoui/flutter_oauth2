# Application
NODE_ENV=development
PORT=3000

# Database - Development
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres         # Your PostgreSQL username
DB_PASS=yourpassword     # Your PostgreSQL password
DB_NAME=oauth2_server_dev # Your development database name

# OAuth Server
ACCESS_TOKEN_LIFETIME=3600 # in seconds (1 hour)
REFRESH_TOKEN_LIFETIME=86400 # in seconds (1 day)
# Add other OAuth specific env vars as needed (e.g., secret keys for signing)

# For Production (Example: DATABASE_URL for services like Heroku)
# DATABASE_URL="postgres://user:password@host:port/database"